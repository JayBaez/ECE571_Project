# Problem 1 — Supervised Classification

**Status: NOT STARTED.**

This folder will eventually contain the code for Problem 1: predicting
a discrete label (sky-condition or generation-regime) from weather and
irradiance features.

Before writing any code here, read:
- `course_context/TEACHER_EXPECTATIONS.md` (Problem 1 section) for the
  exact label definitions and leakage rules.
- `course_context/EXPERIMENT_PLAN.md` (Problem 1 section) for the
  proposed models, metrics, and risks.

This code should reuse `src/data_loader.py`, `src/preprocessing.py`,
`src/feature_engineering.py`, `src/splitting.py`, `src/evaluation.py`,
and `src/visualization.py` rather than reimplementing that logic here.
