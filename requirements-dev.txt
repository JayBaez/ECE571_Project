# Extra packages needed only for DEVELOPING this project (running the
# test suite), not for running the framework itself. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=7.0
